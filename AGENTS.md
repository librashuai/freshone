# 项目开发约定

- 修改代码前先阅读相关实现和测试，保持 Fresh Editor 0.5.1 插件 API 兼容。
- Find in Files 必须通过 `rg`（ripgrep）搜索文件内容，不得退回 `fd` 枚举文件后逐个读取搜索；保留忽略规则、字面量不区分大小写搜索、10 MiB 文件上限和 10000 条结果上限。Find File 则通过 `fd` 按文件名正则查找，保持两个命令的用途分离。
- 改动关键逻辑时添加或更新 `tests/` 中的回归测试，覆盖成功、失败及取消等边界行为。
- **每次提交前必须运行 `npm test` 并确保所有测试通过**；如果运行环境没有所需工具，明确说明未验证的项目，不得声称测试通过。测试需要 Node.js 24+；集成测试在缺少 `rg` 或 `fd` 时会分别跳过，但使用相应功能时仍必须安装对应工具。
- 修改功能或依赖时同步更新 `README.md`。
